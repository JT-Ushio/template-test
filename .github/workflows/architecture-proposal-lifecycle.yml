# name: Architecture proposal lifecycle

# on:
#   issues:
#     types: [opened, reopened, labeled, closed]
#   pull_request_target:
#     types: [opened, edited, synchronize, reopened, closed]
#   workflow_dispatch:

# permissions:
#   contents: read
#   issues: write
#   pull-requests: write

# concurrency:
#   group: architecture-proposal-${{ github.event.issue.number || github.event.pull_request.number || github.run_id }}
#   cancel-in-progress: false

# jobs:
#   lifecycle:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout trusted default-branch automation
#         uses: actions/checkout@v4
#         with:
#           ref: ${{ github.event.repository.default_branch }}
#           fetch-depth: 1
#           persist-credentials: false

#       - name: Test lifecycle state machine
#         run: node --test .github/scripts/architecture-proposal-lifecycle.test.cjs

#       - name: Apply lifecycle transition
#         uses: actions/github-script@v7
#         with:
#           script: |
#             const lifecycle = require("./.github/scripts/architecture-proposal-lifecycle.cjs");
#             await lifecycle.run({ github, context, core });
